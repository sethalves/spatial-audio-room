{
  "name": "basic-example",
  "productName": "HiFi Basic Example",
  "version": "0.3.0",
  "description": "A demonstration of using spatial audio with agora",
  "private": true,
  "scripts": {
    "client": "npm run clean && webpack --env dev",
    "client:dev": "npm run clean && webpack --env dev",
    "client:prod": "npm run clean && webpack --env prod",
    "clean": "rm -rf dist"
  },
  "author": "High Fidelity Inc",
  "license": "...",
  "dependencies": {
    "hifi-spatial-audio": "file:../.."
  },
  "devDependencies": {
    "copy-webpack-plugin": "^10.2.4",
    "ts-loader": "^9.2.6",
    "typescript": "^4.4.4",
    "webpack": "^5.64.0",
    "webpack-cli": "^4.9.1"
  }
}
